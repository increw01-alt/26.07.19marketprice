# 인계 메모 — 핫딜/가격비교 확장 (2026-07-28 검토 → 2026-07-31 1차 구현 완료)

> 이 문서는 원본 기획서(`~/Downloads/modoosise-핫딜연계-기획서.md`)를 검토하고
> 방향을 수정한 결과다. **다음 세션은 이 문서부터 읽으면 바로 이어서 작업 가능.**

---

## 0. 구현 현황 (2026-07-31)

### ✅ 핫딜 피드 `/hotdeal` 배포 완료 — https://modoosise.com/hotdeal
- `scripts/fetch-hotdeals.mjs` — **뽐뿌·루리웹 공식 RSS**에서 딜 파싱. 키 불필요.
  제목에서 판매처(`[대괄호]`)·가격(`(…원)`) 추출. 링크 기준 중복 제거, 최신 40건.
  → `data/hotdeals.json`. 매시간 워크플로 실행. 로컬에서도 그냥 `node`로 실행 가능.
- `hotdeal.html` + `pageHotdeal`(pages.js) + "핫딜" 메뉴(flame 아이콘) + `.deal-*` 스타일.
- **RSS = 사이트가 신디케이션용으로 공식 제공** → HTML 크롤링보다 저작권/ToS 위험 낮음.
  제목·링크만 사용(본문 미전재), 화면에 출처·원문 링크 명시. (결정 C의 안전판 준수)
- 소스 추가는 `SOURCES` 배열에 `{id,name,url}` 추가(RSS 2.0 표준이면 파서 재사용).

### ⏭️ 다음: 2단계 — 핫딜에 가격비교 연계 (아직 안 함)
사용자 요청 순서 = "핫딜 먼저(완료) → 그 위에 가격비교". 구상:
- 핫딜 각 딜의 상품명 → 네이버 쇼핑 API로 조회해 "핫딜가 vs 현재 최저가" 대비 표시.
- 주의: RSS 상품명이 지저분해(용량·수량·판매처 섞임) 그대로 검색하면 매칭률 낮음.
  → 핵심 키워드 추출 or 딜별 수동 매핑 필요. API 호출량(딜 40건×시간당) 관리도.

### ✅ `/shopping` 가격비교 페이지 배포 완료 — https://modoosise.com/shopping

- `scripts/fetch-shopping.mjs` — 네이버 쇼핑 검색 API로 상품별 판매처 최저가 수집.
  매시간 워크플로에서 실행(`data/shopping.json`).
- `shopping.html` — 가격비교 페이지 (SEO 메타·JSON-LD·breadcrumb 포함).
- `assets/pages.js` `pageShopping` 렌더러 + 라우트 / `assets/app.js` "가격비교" 메뉴 + cart 아이콘.
- `assets/style.css` `.shop-*` 카드 스타일. `sitemap.xml` `/shopping` 등록.
- **GitHub Secrets `NAVER_ID`/`NAVER_SECRET` 등록 완료.** 네이버 앱명 "한국상품권협회".
- 검증: 라이브 10개 상품·실제 최저가·판매처 링크·이미지 로드 모두 정상 확인.

### ⚠️ 구현하며 겪은 함정 (재작업 시 주의)
- **`sort=asc`(최저가순) 금지.** 단품·샘플·액세서리가 최저가로 잡혀 "350원 코카콜라
  24캔" 같은 오기가 나온다. → **`sort=sim`(정확도순) + 상품별 `min` 하한선**으로 거른다.
  QUERIES 각 항목의 `min` 값이 신뢰도의 핵심 (기존 상품권 `sane()` 필터와 같은 개념).
- **네이버 이미지 URL에 쿼리스트링(`?cb=` 등) 붙이면 깨진다.** CDN이 원본 URL만 받음.
- **상품 추가/수정은 `scripts/fetch-shopping.mjs` 상단 `QUERIES` 배열만 편집**하면 된다
  (id·name·query·min[·max]). 지금은 생필품/먹거리 10종 시드.

### 다음 확장 아이디어 (2차)
- [ ] 상품 목록 확대·카테고리화(생필품/가전/상품권 등 탭 분리).
- [ ] 매입 시세(상품권)와 엮은 "실익 계산" — 원 기획서의 `/calc/` 아이디어.
- [ ] `/event/` 는 여전히 보류 (아래 결정 C 참고 — 크롤링은 발굴 신호로만).
- [ ] README.md·PROGRESS.md 본문에 가격비교 페이지/데이터소스 반영(현재 미반영).

---

## 1. 한 줄 요약

핫딜 확장을 **"핫딜 재유통"이 아니라 "가격비교 시세 데이터"로 재프레임**하기로 결정.
지금 사이트가 금값·환율을 "이 시각 기준 얼마"로 보여주는 것과 **같은 성격**으로 만든다.

---

## 2. 오늘 확정한 결정 (원본 기획서 대비 수정 사항)

### ✅ 결정 A — "추천 딜"이 아니라 "가격 시세"로 프레임
- 이유: "우리가 검증한 딜 추천" 모델은 **보증 책임**이 생김(협회 명의라 더 무거움).
- 대신 "이 상품 = 여기 21,760원 / 네이버 최저가 23,100원"을 **시각(as-of) 붙여 사실만 표시**.
- 이러면 추천이 아니라 데이터 → 기존 시세 페이지 면책과 동일 수준으로 관리 가능.
- 각 항목에 문구 필수: **"○○시 기준 자동 수집 가격 · 참고용 · 판매/중개 아님"**.
- → 협회 책임 논점은 "명의"가 문제가 아니라 "추천처럼 쓰느냐 데이터로 쓰느냐"였고,
  데이터로 가면 해소됨.

### ✅ 결정 B — 크롤링·이미지 긁기 금지, 공식 API 사용 (가장 중요)
목표 화면 = 네이버 가격비교 스타일(상품명·이미지·최저가·판매몰 비교).
이걸 **어떻게 얻느냐**가 합법/위험을 가름:

- ❌ 네이버 검색 HTML·G마켓 페이지 크롤링 → 네이버 DB라 약관 위반 + 적극 차단됨.
- ❌ 상품 대표이미지를 긁어 우리 서버에 재호스팅 → **이미지 저작권** 문제
  (가격·상품명은 사실이라 자유롭지만 **이미지는 별개**).
- ✅ **네이버 쇼핑 검색 API**(개발자센터)를 쓴다:
  - 상품명·최저가/최고가·**이미지 URL**·판매몰·상품링크를 정식 제공.
  - 무료, 하루 25,000회, 출처표기 조건. 이미지 사용권 포함.
  - 지금 오피넷/ECOS 키 받아 쓰는 것과 **완전히 같은 패턴** → 기존 구조에 그대로 얹힘.
  - (선택) 쿠팡파트너스 API — 승인 필요, 제휴링크·이미지·가격.

### ✅ 결정 C — 커뮤니티 크롤링(알구몬/핫딜방)은 "발굴 신호"로만
- 역할 분리: **크롤링 = "지금 뭐가 딜로 뜨나" 힌트만** / **표시 데이터 = 네이버 쇼핑 API에서 정식 취득**.
- 재게시가 아니라 발견용이라 저작권·SEO(긁은 콘텐츠 판정) 리스크 거의 소멸.
- 남는 건 발굴용 크롤링의 ToS 회색지대뿐(재게시 아니라 실무 위험 낮음).
- 참고 오픈소스: `krepe90/user-hotdeal-bot` (Python, 8개 커뮤니티 핫딜판 1분 주기 크롤 → 텔레그램, MIT).
  단, 봇 라이선스(MIT)와 크롤 대상 사이트 ToS는 별개임에 유의.

---

## 3. 원본 기획서에서 살아있는/버리는 것

- 살림: "정보 먼저, 수익화 나중" 원칙 / `/calc/` 실익 계산기 / `/discount/` 가격비교 매트릭스
  아이디어 / T4(현금화) 키워드 배제 / 시세↔할인 상호 내부링크.
- 수정: `/event/` 자동 크롤 재발행 → **네이버 쇼핑 API 기반 가격비교로 대체**.
- 주의(원본 지적): "직접 검증 발행"은 인건비·정확성 책임 때문에 확장 안 됨 →
  범위를 **상품권/특정 카테고리로 좁혀** 관리 가능하게.
- 과장 교정: `/calc/`는 복제 불가 독점이 아님. 진짜 해자는 **매입 시세 데이터 + 신뢰**.

---

## 4. 다음 단계 (여기서부터 이어서)

**막힌 지점: 네이버 쇼핑 검색 API 키가 필요함. (2026-07-30 확인 결과 = 아직 없음)**

### ⚠️ API 키 현황 (코드/설정 확인 완료)
- 이 프로젝트에 등록된 GitHub Secrets는 **3개뿐**: `DATA_GO_KR_KEY`(부동산)·`ECOS_KEY`(금리)·`OPINET_KEY`(기름값).
- **네이버 쇼핑 검색 API 키(`NAVER_ID`/`NAVER_SECRET`)는 없음.** 아직 등록 안 함.
- 헷갈리기 쉬운 것 3가지 (전부 쇼핑 API 아님):
  1. **네이버 서치어드바이저**(`searchadvisor.naver.com`) = SEO 사이트 소유확인. 이미 ✅ 완료.
     → 이걸 "네이버 API 등록"으로 기억하기 쉬운데, **쇼핑 검색 API와 전혀 다른 서비스**. 재사용 불가.
  2. **네이버 금융**(`m.stock.naver.com`) = 금값(KRX) 가져오는 스크래핑. 키 없이 호출.
  3. `NAVER`(035420) = 그냥 종목 리스트의 네이버 주식.
- 결론: 쇼핑 검색 API는 **`developers.naver.com`에서 별도 신규 등록** 필요. 서치어드바이저 계정으론 안 됨.

### 할 일
1. [ ] **네이버 개발자센터에서 앱 등록 → Client ID / Secret 발급**
   - https://developers.naver.com/apps/#/register  (먼저 https://developers.naver.com/apps 에서 기존 앱 있는지 확인)
   - 사용 API: "검색" 추가. (키는 **GitHub Secrets로만** 전달, 코드/데이터에 절대 금지 —
     기존 원칙 준수. 예: `NAVER_ID`, `NAVER_SECRET`)
2. [ ] 키 확보 후 Claude에게: **`scripts/fetch-shopping.mjs` 초안 + 데이터 스키마 + 페이지 스펙** 요청
   - 수집 스크립트: 대상 상품(또는 키워드) 리스트 → 네이버 쇼핑 검색 API 조회 → `data/shopping.json` 저장.
     (API 응답 필드: title·lprice/hprice·image(URL)·mallName·link)
   - 페이지: `/discount/` 또는 `/price/` 가격비교 표(행=상품, 열=판매몰), 갱신 시각·출처 표기,
     모바일 가로 스크롤, `<body data-page="...">` + `pages.js` 렌더러 패턴.
   - SEO: 페이지별 고유 title/description, 구조화 데이터(Table/FAQPage), 시세↔가격비교 내부링크.
3. [ ] `?v=` 자산 버전 갱신, sitemap.xml에 새 페이지 추가.
4. [ ] update-data.yml 워크플로에 fetch-shopping 스텝 + NAVER Secret env 추가.

---

## 5. 놓치면 안 되는 것 (기존 프로젝트 원칙과 동일)
- 인증키는 GitHub Secrets / Cloudflare 바인딩만. 코드·데이터에 절대 금지.
- 데이터는 `/data/*.json` fetch로 그림 → robots.txt에서 `/data/` 막지 말 것.
- 자산 변경 시 HTML `?v=` 동반 갱신.
- 배포는 main push 자동(Cloudflare Pages). 새 페이지 추가 시 sitemap 동반.
