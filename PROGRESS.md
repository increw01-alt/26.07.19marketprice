# 진행 상황 · 결정 기록 (PROGRESS)

이 문서는 **무엇을, 왜 그렇게 결정했는지**를 기록합니다.
"어떻게 만들었는지"는 코드와 `README.md`에, 여기엔 결정과 배경만 남깁니다.

---

## 1. 프로젝트 개요

| 항목 | 값 |
|---|---|
| 서비스명 | **모두의 시세** — 대한민국 모든 시세 한눈에 |
| 대표 도메인 | **https://modoosise.com** |
| 운영 주체 표기 | 한국상품권협회 (about 페이지) |
| 호스팅 | Cloudflare Pages (main 브랜치 자동 배포) |
| 저장소 | github.com/increw01-alt/26.07.19marketprice (공개) |
| 구조 | 빌드 도구 없는 순수 정적 사이트 + GitHub Actions 수집 + Cloudflare Functions/D1 |

---

## 2. 핵심 결정 로그

### 방향·브랜딩
- **한 페이지 → 메뉴별 다중 페이지로 개편.** 검색 유입 시 "주식시세"는 `/stock`,
  "코인시세"는 `/coin` 처럼 **해당 페이지로 바로** 들어오게 하기 위함. 각 페이지에
  고유 title·description·구조화 데이터를 넣음.
- **사이트명을 "시세" → "모두의 시세"로 변경.** "시세" 단독은 일반명사라 브랜드
  검색이 불가능. "모두의 시세"는 고유 조합이라 브랜드 검색 유입을 만들 수 있음.
- **도메인 `k-coin.kr` → `modoosise.com` 으로 교체.** k-coin 이 과거 "케이코인"
  다단계 사기 사건과 이름이 겹쳐 검색 시 부정적 정보가 노출됨. 색인 전에 교체해
  손실 없이 정리. (구 도메인은 리다이렉트 폴백만 남김)
- **디자인: ui-ux-pro-max 스킬의 Data-Dense Dashboard 규칙 채택.** 네이비(다크)를
  기본 테마로, 라이트는 토글로만. 로고는 3봉 캔들차트(3안).

### 데이터 출처 (무료·키 최소 원칙)
- **stooq → Yahoo Finance 로 교체.** stooq 가 봇 차단(PoW) 도입. 우회하지 않고
  대체 소스로 이동.
- **동행복권 구 API 폐기 → 신 API(`selectPstLt645InfoNew.do`).** 사이트 개편으로
  구 엔드포인트가 폐기됨.
- **상품권은 "직접 집계"가 아니라 "공개 시세 취합".** 씨티페이·최고·명인 업체의
  공개 시세 페이지를 모아 비교. about 문구도 이 성격에 맞게 조정(과장 배제).
- **금시세는 국내 실거래가(KRX) 우선.** 기존 국제가×환율 환산은 네이버 등 국내
  실거래가와 ~1% 차이. 사용자 기대는 국내가이므로 KRX(네이버 금융 경유) 를
  전면에, 국제가는 뒤에. 금 1돈도 KRX 기준.
- **부동산: 전남광주통합특별시(2026-07-01) 재코드 대응.** 광주(29)·전남(46)이
  코드 12 로 통합됨. API 가 과거 월까지 재분류해 구 코드는 전 기간 0건.
  → 새 코드 반영 + 지도 연결을 sido 이름 매칭으로 전환.

### 로또 기능 (여러 번 방향 전환한 이력)
- 처음: 사이트가 매주 공식 추천 5게임을 내고 성적 기록 → **폐기.**
- 다음: 방문자가 버튼을 눌러 직접 뽑는 방식으로 전환. 확률을 높이지 않음을 명시.
- 성적 기록: **localStorage(개인) + D1(공개 피드) 병행 채택.** 방문자가 다시
  오지 않아도 결과가 남도록 서버 저장 필요 → Cloudflare D1 도입.
- 추천 안내 문구: 통계 설명 → **위트 있는 행운 메시지 + 건전 구매 안내**로 교체.
- 1인당 구매 게임 수 기본값 5 → **10(1만원)** + 안내 문구.

### 백엔드 (Cloudflare D1)
- **로또 공개 당첨 피드용으로 D1 도입.** 무료 한도(요청 10만/일, D1 쓰기 10만/일,
  저장 5GB)로 충분. 초과해도 과금 아니라 차단만. 결제수단 미등록.
- 채점은 **읽을 때 지연 채점(idempotent).** 별도 크론·시크릿 불필요.
- 이 D1(대시보드 바인딩명 `DB`, DB명 `유행하는`)은 **추후 댓글 게시판에 재사용** 예정.

---

## 3. 현재 구조

### 페이지 (12개)
`index`(홈) · `coin` · `stock` · `kosdaq` · `fx` · `metal` · `energy` ·
`macro` · `giftcard` · `realestate` · `lotto` · `about`

공통 셸(헤더·메뉴·푸터)은 `assets/app.js` 에서 1회 정의, 각 페이지는
`<body data-page="...">` 로 자신을 식별. `assets/pages.js` 가 렌더러를 디스패치.

### 데이터 소스

| 분야 | 출처 | 키 | 주기 |
|---|---|---|---|
| 코인 | 업비트 (거래대금 상위 10) | — | 매시간 |
| 주가지수·해외 | Yahoo Finance | — | 매시간 |
| 국내 종목(코스피·코스닥) | Yahoo Finance | — | 매시간 |
| 환율 | Yahoo Finance | — | 매시간 |
| 금(국제)·은·백금 | Yahoo Finance | — | 매시간 |
| **금(국내)·금 1돈** | **한국거래소(KRX) / 네이버 금융** | — | 매시간 |
| 유가·원자재 | Yahoo Finance | — | 매시간 |
| 주유소 기름값 | 오피넷 | `OPINET_KEY` | 매시간 |
| 국내 금리 | 한국은행 ECOS | `ECOS_KEY` | 매시간 |
| 상품권 | 씨티페이·최고·명인 (공개 시세) | — | 매시간 |
| 부동산 실거래가 | 국토교통부 | `DATA_GO_KR_KEY` | 하루 1회 |
| 로또 | 동행복권 신 API | — | 매시간(누적) |
| 뉴스 | 구글뉴스 RSS (메뉴별) | — | 매시간 |
| 김치프리미엄 | 업비트 + Coinbase 파생 | — | 매시간 |

### 백엔드
- `functions/api/lotto/pick.js` — 방문자 뽑은 번호 저장 (POST)
- `functions/api/lotto/results.js` — 당첨 공개 피드 (GET, 읽을 때 채점)
- `schema.sql` — D1 스키마 (`lotto_picks`)

---

## 4. 운영·설정 상태

| 항목 | 상태 |
|---|---|
| Cloudflare Pages 배포 | ✅ main push 자동 |
| 커스텀 도메인 | ✅ modoosise.com / www (구 k-coin.kr 리다이렉트) |
| Google Analytics (GA4) | ✅ `G-D5NLQZ3DST` 전 페이지 |
| Google Search Console | ✅ 소유확인(DNS) + 사이트맵 제출 |
| 네이버 서치어드바이저 | ✅ 소유확인 + 사이트맵 + RSS |
| SEO | ✅ canonical·OG·breadcrumb·sitemap·robots·RSS |
| GitHub Secrets | ✅ `ECOS_KEY` `OPINET_KEY` `DATA_GO_KR_KEY` |
| Cloudflare D1 | ✅ DB명 `유행하는`, 바인딩명 `DB`, Pages 연결·검증 완료 |

---

## 5. 남은 작업 / 아이디어

- [ ] **댓글 게시판** — 각 메뉴 하단. D1 재사용(테이블 추가). 스팸·삭제 관리 필요.
- [ ] **명인상품권 파서 점검** — 사이트 구조가 바뀌어 수집 실패한 적 있음(자동 방어됨).
- [ ] www → apex 301 을 Cloudflare Redirect Rule 로 정식 전환 (현재는 JS 폴백).
- [ ] 부동산·주식 개별 종목 확대 (현재 지수·주요 종목 위주).
- [ ] 트래픽 증가 시: 쿠키 동의 배너, D1 오래된 낙첨 기록 정리.

---

## 6. 주의사항 (놓치기 쉬운 것)

- **인증키는 절대 코드·데이터에 넣지 않는다.** GitHub Secrets / Cloudflare 바인딩만.
- **robots.txt 에서 `/data/` 를 막지 않는다.** 콘텐츠가 전부 JSON fetch 로 그려짐.
- **자산(css/js) 변경 시 HTML 의 `?v=` 쿼리를 함께 갱신**해야 캐시가 안 엉킴.
- **금액 계산·행정코드는 업스트림 오기·개편을 항상 의심.** (상품권 오기 필터,
  전남광주 재코드, Yahoo 전일종가 함정 등 실제로 겪음 — README 참고.)
